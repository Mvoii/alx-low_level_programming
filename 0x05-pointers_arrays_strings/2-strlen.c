#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: string parameter input
 *
 * Return: length of string
*/

int _strlen(char *s)
{
	int len = 0;
	for (*str; *str != '\0'; *str++)
	{
		len++;
	}
	return (len);
}
